# 🧮 Project 02 – SimpleCalculator  

---

## 🇩🇪 Beschreibung  
Ein einfacher Konsolenrechner, der zwei Zahlen und einen Operator (+ − × ÷) einliest und das Ergebnis ausgibt.  
Das Programm prüft auf falsche Eingaben (z. B. Division durch Null) und zeigt entsprechende Fehlermeldungen.  

**Kernpunkte:**  
- Benutzer-Eingabe mit `Scanner`  
- Verzweigungen mit `switch-case`  
- Fehlerbehandlung mit `try-catch`  

---

## 🇨🇳 项目说明  
一个简单的控制台计算器：输入两个数字和一个运算符（+ − × /），  
输出计算结果，并在输入错误（例如除以 0 或无效运算符）时给出提示。  

**重点内容：**  
- 使用 `Scanner` 获取用户输入  
- 使用 `switch-case` 结构实现运算逻辑  
- 通过 `try-catch` 进行异常处理  

---

## 🇬🇧 Description  
A simple console-based calculator that reads two numbers and an operator (+ − * /),  
then prints the result. It handles invalid inputs and division by zero gracefully.  

**Key Concepts:**  
- User input with `Scanner`  
- Conditional logic using `switch-case`  
- Exception handling with `try-catch`  

---

## ⚙️ How to Run / 运行方法 / Ausführen  

```bash
# Compile
javac SimpleCalculator.java

# Run
java SimpleCalculator
